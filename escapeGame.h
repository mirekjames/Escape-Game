#pragma once

enum shape{
  TRIANGLE,
  CIRCLE,
  RECTANGLE
};

enum pointing{
  LEFT,
  RIGHT,
  UP,
  DOWN
};

enum view{
  FRONTWALL,
  RIGHTWALL,
  BACKWALL,
  LEFTWALL,
};

enum item{
  NONE,
  KEY,
  PIRANHA,
  NET,
  KEYTWO,
  KEYTHREE,
  SCREWDRIVER
};

enum zoom{
  NOT,
  SAFE,
  PAINTING,
  WALLCLUE,
  BARREL,
  CLOCK,
};
